<VirtualHost *:80>
        ServerAdmin griccardi@fsu.edu
        ServerName services.morphbank.net

        TimeOut 900

        ProxyPreserveHost On
        ProxyPass / http://localhost:8080/ 
        ProxyPassReverse / http://localhost:8080/ 
        ProxyTimeout 900

#       RewriteEngine On
#       RewriteRule  [R]
#       Redirect permanent / http://services.morphbank.net:8080/

        ErrorLog /data/log/httpd/services-error_log 
        TransferLog /data/log/httpd/services-access_log 
</VirtualHost>

